# Scripts

Stable deterministic helpers should live here once they are reused across runs.

Initial candidates:

- `scan_assets.py`: richer ffprobe metadata extraction.
- `transcribe_audio.py`: Whisper adapter.
- `assemble_ffmpeg.py`: FFmpeg render adapter.
- `validate_subtitles.py`: reusable subtitle-safe-area checks.

